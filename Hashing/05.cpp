/* 
    PROBLEM STATEMENT
    Given an array and a sum x find if there is subarray where sum is aqual to x.

    I/P: arr[] = {5,3,8,-2,8,10}
    O/P: true
*/
#include<iostream>
#include<unordered_set>
using namespace std;
int main(){
    int n,x;
    cout<<"Enter the number of elements: ";
    cin>>n;
    int arr[n];
    cout<<"Enter the elements: ";
    for(int i=0;i<n;i++)
        cin>>arr[i];
    cout<<"Enter the value of x: ";
    cin>>x;
    
}